.card
  .card-header
    %h3
      = @cardtec_node.try(:title)
      %small= @cardtec_node.neo_id
    %ul
      %li
        = @cardtec_node.ident rescue 'n/a'
      %li
        = @cardtec_node.labels.join(', ')
    %hr
      = @cardtec_node.to_html

  .card-editor
    %big
      = text_area_tag :code, @cardtec_node.to_yaml, rows: 30, cols: 80
