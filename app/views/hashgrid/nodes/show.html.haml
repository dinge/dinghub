.card
  .card-header
    %h3
      = @node.title
      %small= @node.neo_id
    %ul
      %li
        = @node.ident rescue 'n/a'
      %li
        = @node.labels.join(', ')
    %hr
    %dl
      - @node.props.each do |key, value|
        %dt
          = key
        %dd
          = value

  .card-editor
    %big
      = text_area_tag :code, @cardtec_node.to_s, rows: 30, cols: 80
