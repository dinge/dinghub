:ruby
  form_options = node.persisted? ? { method: :put } : { method: :post, url: raw_node_nodes_path }

= simple_form_for :cardtec_node, form_options do |f|
  = trio_panel('edit yaml', nil, f.button(:submit, 'save', class: 'success radius right'))
  = f.text_area :yaml, value: node.ctn.to_yaml, rows: 18, cols: 80
