

/ %a{ href: me_root_path, 'data-reveal-id' => :dialog, 'data-reveal-ajax' => 'true' }
/   dialog

#dialog.reveal-modal{'data-reveal' => true}

/ #operations_control
/   %ul.inline-list.cards
/     - @nodes.map.first(5).each do |n|
/       %li
/         = render 'small_card', node: n

#operations_control
  .left
    = render 'small_card', node: @nodes.first
  .center
    = render 'small_card', node: @nodes.first
  .right
    = render 'small_card', node: @nodes.first


%ul.inline-list.cards#main_card_list
  = render 'cards', nodes: @nodes



- content_for :javascript do
  $('.card a').attr('data-reveal-id', 'dialog').attr('data-reveal-ajax','true');
  $('.side-nav a:last').attr('data-reveal-id', 'dialog').attr('data-reveal-ajax', 'true');