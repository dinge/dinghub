:ruby
  link_path ||= current_show_path(node)

.card.small-card{class: dom_class(node, :card), 'data-card-path' => current_show_path(node), 'data-card-uuid' => node.uuid}
  .card-header
    %h5
      = link_to(link_path) do
        = node.human_title






  .card-body
    %hr
    - if node.image?
      .main-image
        = link_to(link_path) do
          = image_tag(node.image.small.url)

  .card-footer
    / %small.left
    /   = node.labels.last
    / %small.right
    /   = node.neo_id

