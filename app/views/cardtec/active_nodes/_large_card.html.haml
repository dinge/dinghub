.card.large-card
  .card-header
    %h2
      = render 'card_header', node: node
    %hr
  .card-body
    = render 'property_tabs', node: node
    = render 'related_nodes_tabs', node: node
    - if node.image?
      %span.th
        = image_tag(node.image.url)


