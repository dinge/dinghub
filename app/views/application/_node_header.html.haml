= link_to(current_show_path(node.neo_id)) do
  %small.left
    = node.labels.join(', ')
  %small.right
    = node.neo_id
  %br
  = (node.title rescue node.neo_id)
  %br
  %small= node.ident rescue ''
