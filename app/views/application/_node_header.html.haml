:ruby
  link_path ||= current_show_path(node)

= link_to(link_path) do
  %small.left
    = node.labels.join(', ')
  %small.right
    = node.neo_id
  %br
  = (node.ctn.title rescue node.neo_id )
  %br
  %small= node.ident rescue ''
